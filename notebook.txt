graph:
 """g (StateGraph对象)
├─ state_schema → InvestState  # 记住state的模板
├─ nodes → {"collect": collect函数, ...}  # 你的节点注册表
└─ edges → [(START,"collect"), ...]       # 流转连线"""



 """
    g.nodes = {
    "collect": collect函数对象,
    "analyze": analyze函数对象,
    "compare": compare函数对象,
    "report": report函数对象
}
"""

"""2. START 和 END 是 LangGraph 预定义的特殊节点。START
    表示"流程从这里开始"，END 表示"到这里结束"。它们不是真实节点，是标记。
    g.edges = [
    (START, "collect"),
    ("collect", "analyze"),
    ("analyze", "compare"),
    ("compare", "report"),
    ("report", END)
]
"""

先命名,再自定义执行流程
add_node("节点名字", 函数本身)
add_edge 完全不看 Python 函数叫什么，**只认 add_node 登记的字符串 key**。


5---建main.py
 1. sys.argv 是命令行参数列表。sys.argv[0] 是脚本名，sys.argv[1]
  是第一个参数（即股票代码）。所以 python -m invest_agent.main 600519
  里，sys.argv[1] 就是 "600519"。
  2. sys.exit(1) 表示"异常退出"（1 是错误码，0
  是正常）。当用户没传参数时，打印用法说明然后退出。
  3. result['report_path'] —— 你现在能从最终 state
  里取任何字段，因为之前验证过状态都合并好了。


 2.sys 模块用来获取**终端命令行参数**、主动退出程序

 3.7. `code = sys.argv[1]   取出终端传入的第一个参数，赋值给变量 code，就是股票代码字符串

 4---
 `app.invoke(初始状态)`：**给机器喂入初始数据，完整跑一遍整个工作流，直到走到 END 结束，返回最终 state**
 `app.invoke({"code": code})` → 构造初始 state,只填充一个code后面再collect中根据股票代码收集数据

 5---test_flow,测试链路能否跑通
 report 节点函数是关键,获取某股票的文件路径


 阶段一到此结束,
  - LangGraph 的 5 个核心概念：State / Node / Edge / Graph(图纸),详情去看graph的内容 /invoke(接受传入的数据没传入的填充None)
  - 状态自动合并：每个节点只返回部分更新，最终 state 自动拼全
  - src 布局 + pyproject + editable 安装的现代 Python 工程姿势
  - 节点边界设计：数据采集是唯一碰数据源的，其他节点只读文件
  - `StateGraph(g)`：图纸，画流水线；
  - `compile()`：按照图纸造出真实流水线机器 `app`；
  - `app.invoke(初始数据)`：把原料丢进流水线，按下开关，完整跑完生产，产出成品 (result)。



  阶段二
  1.建akshare探针获取股票信息
  2.VFS模块:把"目录在哪、文件怎么读写"收口到一个模块，其他代码只调接口，不碰路径字符串。
  `json.dump(内存数据, 文件对象, ...)`，把内存数据写到磁盘文件
  `os.path.dirname(path)`：**提取文件路径的父目录**
   save_json做持久化,用的时候再调用read读出来
   3.rename()内加一个字典包含旧列名和新列名
   字典方法 `.values()`：**拿到字典所有右边的值（新列名
   pandas 表格后面跟方括号 `[列名列表]`：**列筛选语法**
    只保留方括号里面写的这些列，表格里其他全部列直接丢掉
    selected.iloc(0)按位取第零行


 4.封装 AKShare 采集 tool
    1先把股票号转换为能直接查询的股票代码
    2使用akshare函数查询对应股票的数据
    3调用清洗函数对数据进行清洗


阶段5
对clean函数测试在test,建立虚假数据在数据内放入垃圾看清洗效果
利用assert函数,绝对值函数abs(),极小数字1e-9,